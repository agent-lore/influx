# Ralph Progress Log
Started: Sat Apr 25 19:50:21 UTC 2026

## Codebase Patterns
- Pydantic schemas live in `src/influx/schemas.py`; use `Field(min_length=, max_length=)` for list bounds
- Test style: classes grouped by `TestXPositive` / `TestXNegative` with `pytest.raises(ValidationError)` for negative cases
- Test file naming: `tests/unit/test_<topic>.py`
- Existing enrich.py has a stub `Tier1Result` dataclass + `tier1_enrich` function that PRD 07 replaces
- Full test suite (878 tests) runs in ~93s via `uv run pytest`
- Ruff + pyright are the quality gates (`uv run ruff check`, `uv run ruff format --check`, `uv run pyright`)
- Tier3Extraction uses `field_validator` with `mode="before"` for trim+truncate, `mode="after"` for non-empty check
- Use `_valid(**overrides)` helper in test files for concise payload construction with overrides
- HTTP mocking pattern: `@patch("influx.extraction.html.guarded_fetch")` + `FetchResult(body=..., status_code=200, content_type="text/html; charset=utf-8", final_url=...)`
- `ExtractionError` in `errors.py` has fields: url, stage, detail — use stage="min_length" for threshold rejections
- trafilatura.extract(html, favor_recall=True) for article extraction; returns None on failure
- `extract_pdf(pdf_bytes, source_url=...)` takes raw bytes, not URLs — caller owns the fetch; uses `pypdf.PdfReader(io.BytesIO(...))`
- PDF test fixtures: `sample.pdf` (valid, ~312 chars), `blank.pdf` (no extractable text)
- `extract_article()` in `extraction/article.py` reuses `_strip_tags` and `_clean_html_fragments` from `extraction/html.py` — same tag-stripping + fragment cleanup, different min-length gate (`min_web_chars` default 500)
- Article test fixtures: `web_article.html` (good), `short_web_article.html` (below 500 chars), `web_with_script.html` (has all 4 dangerous tags)
- `_call_json_model(config, slot_name, prompt)` in `enrich.py` is the reusable JSON-mode inference helper — resolves provider/model from config, returns parsed dict; patch it in tests
- `LCMAError` with `stage="validate"` signals schema validation failure; `stage="http"` for transport; `stage="parse"` for JSON parse failures
- Model slots are accessed via `config.models["enrich"]` / `config.models["extract"]`; providers via `config.providers[slot.provider]`
- Conftest fixture `influx_config_env` provides `models.enrich` and `models.extract` slots pointing to `test-provider`
- `render_note` canonical section order: Archive → Summary → Full Text → (Tier 3 sections) → Profile Relevance → User Notes
- `tier1_enrichment: Tier1Enrichment | None = None` parameter on `render_note` — when provided, renders structured `## Summary` with `### Contributions`, `### Method`, `### Result`, `### Relevance`; when absent + no plain summary, section omitted; when absent + plain summary, backward-compat plain rendering
- `extract_arxiv_text(arxiv_id, config)` in `extraction/pipeline.py` — HTML→PDF cascade; returns `ArxivExtractionResult(text, source_tag)` or raises `ExtractionError`
- `build_arxiv_note_item(item, score, confidence, reason, profile_name, config)` in `sources/arxiv.py` — builds complete `ProfileItem` dict with extraction, tags, and rendered note
- arXiv HTML URL format: `https://arxiv.org/html/{arxiv_id}`, PDF: `https://arxiv.org/pdf/{arxiv_id}.pdf`
- `ProfileThresholds` has `relevance=7, full_text=8, deep_extract=9` defaults; score >= full_text triggers extraction
- Tags set on initial write: `profile:X`, `arxiv-id:X`, `source:arxiv`, `ingested-by:influx`, `schema:v1`, `cat:X`, `text:X`, optionally `full-text`, `influx:deep-extracted`, `influx:repair-needed`
- Enrichment wiring in `build_arxiv_note_item`: `tier1_enrich` when score >= relevance, `tier3_extract` when score >= deep_extract AND extracted_text available; failures caught as `LCMAError`
- When tier1 attempted but failed, must pass `summary=""` to suppress plain-text `## Summary` rendering (AC-07-A)
---

## 2026-04-25 - US-001
- Added `Tier1Enrichment` Pydantic schema to `src/influx/schemas.py` with `contributions: list[str]` bounded [1,6], `method: str`, `result: str`, `relevance: str`
- Created `tests/unit/test_tier1_schema.py` with 9 tests (3 positive, 6 negative)
- Files changed: `src/influx/schemas.py`, `tests/unit/test_tier1_schema.py`
- **Learnings for future iterations:**
  - Pydantic `Field(min_length=1, max_length=6)` on `list[str]` enforces list length bounds cleanly
  - The existing `FilterResult`/`FilterResponse` in schemas.py follows the same bounded-list pattern — good reference
  - Virtual env may need recreation if Python interpreter changed (uv handles this automatically)
---

## 2026-04-25 - US-002
- Added `Tier3Extraction` Pydantic schema to `src/influx/schemas.py` with `claims` [1,10], `datasets`/`builds_on`/`open_questions`/`potential_connections` [0,10], 500-char truncation via `field_validator`, non-empty string validation
- Created `tests/unit/test_tier3_schema.py` with 25 tests (12 positive, 13 negative) covering all list-length bounds and non-empty string validation
- Created `tests/unit/test_tier3_truncation.py` with 9 tests covering 500-char truncation across all fields
- Files changed: `src/influx/schemas.py`, `tests/unit/test_tier3_schema.py`, `tests/unit/test_tier3_truncation.py`
- **Learnings for future iterations:**
  - Pydantic `field_validator` with `mode="before"` runs before Pydantic's own validation — ideal for trim+truncate that must happen before length checks
  - `mode="after"` runs after Pydantic validation — good for semantic checks like non-empty after trim
  - `default_factory=list` on optional list fields avoids mutable default pitfalls and lets callers omit them
  - Full test suite is 863 tests after US-001 + US-002, all passing
---

## 2026-04-25 - US-003
- Added 7 unit tests to `tests/unit/test_config.py`: 3 for ExtractionConfig defaults (min_html_chars=1000, min_web_chars=500, strip_tags=[script,iframe,object,embed]) and 4 for user-supplied overrides flowing through
- No code changes needed — ExtractionConfig already had correct defaults in `src/influx/config.py` and `influx.example.toml`
- Files changed: `tests/unit/test_config.py`
- **Learnings for future iterations:**
  - ExtractionConfig is already fully declared with PRD 07 defaults; US-003 was test-only
  - Config test pattern: `_write_config(tmp_path, toml)` + `monkeypatch.setenv("INFLUX_CONFIG", ...)` + `load_config()`
  - Full test suite is 870 tests after US-001 + US-002 + US-003, all passing
---

## 2026-04-25 - US-004
- Created `tests/unit/test_prompt_contracts.py` with 8 tests validating the shipped tier1_enrich and tier3_extract prompt variable contracts
- Tests load prompt text directly from `influx.example.toml` to verify variable sets match the `REQUIRED_VARIABLES` contract
- Covers: load_prompt returns text, validate_prompt_variables accepts shipped prompts, tampered prompts (extra/missing variable) rejected with PromptValidationError
- No code changes needed — prompts already shipped in `influx.example.toml` with correct variables; `REQUIRED_VARIABLES` already declares the contract in `src/influx/prompts.py`
- Files changed: `tests/unit/test_prompt_contracts.py`
- **Learnings for future iterations:**
  - Shipped prompts live in `influx.example.toml` under `[prompts.tier1_enrich]` and `[prompts.tier3_extract]`
  - TOML multiline strings (`"""..."""`) include leading/trailing newlines — use `.strip()` for comparison
  - `validate_prompt_variables` correctly ignores `{{escaped}}` braces in prompt text (JSON examples in prompts)
  - Full test suite is 878 tests after US-001…US-004, all passing
---

## 2026-04-25 - US-005
- Added `trafilatura>=2.0` and `pypdf>=5.0` to `pyproject.toml` dependencies
- Ran `uv sync` — resolved successfully, installed 17 new packages (trafilatura 2.0.0, pypdf 6.10.2 + transitive deps)
- Verified both imports work: `import trafilatura`, `import pypdf`
- Files changed: `pyproject.toml`, `uv.lock`
- **Learnings for future iterations:**
  - trafilatura brings several transitive deps: lxml, courlan, htmldate, justext, dateparser, babel, charset-normalizer, regex, tld, urllib3
  - pypdf is relatively lightweight with no extra transitive deps
  - Pre-existing pyright errors (110) in test_tier3_schema.py/test_tier3_truncation.py from `_valid(**overrides)` pattern — these were committed in US-002 and are not blocking
  - Full test suite is still 878 tests, all passing
---

## 2026-04-25 - US-006
- Created `src/influx/extraction/__init__.py` (package marker) and `src/influx/extraction/html.py` with `extract_html()` function
- `extract_html()` uses guarded_fetch → tag-stripping via regex → trafilatura.extract → HTML fragment cleanup → min-length gate
- Created 6 HTML test fixtures in `tests/fixtures/extraction/`: good_article, short_article, with_script, with_iframe, with_object, with_embed
- Created `tests/unit/test_html_extraction.py` with 14 tests covering success path, min-length rejection, tag-stripping for all 4 default tags, no-HTML-fragments, and failure propagation
- Files changed: `src/influx/extraction/__init__.py`, `src/influx/extraction/html.py`, `tests/fixtures/extraction/*.html` (6 files), `tests/unit/test_html_extraction.py`
- **Learnings for future iterations:**
  - trafilatura.extract(html_body, favor_recall=True) returns plain text or None; pass HTML as string not bytes
  - Tag stripping uses regex `<tag\b[^>]*>.*?</tag>` with re.DOTALL for content removal + `<tag\b[^>]*/?\s*>` for self-closing
  - `_clean_html_fragments()` strips any residual `<...>` from trafilatura output as a safety net
  - ExtractionError with stage="min_length" signals threshold rejection; callers catch this to fall through
  - pytest.raises(ExtractionError, match="...") matches against the exception *message*, not attributes
  - Full test suite is 892 tests after US-001…US-006, all passing
---

## 2026-04-25 - US-007
- Created `src/influx/extraction/pdf.py` with `extract_pdf(pdf_bytes, source_url=...)` function using pypdf
- `extract_pdf()` accepts raw bytes (not URLs) — caller owns the HTTP fetch via guarded_fetch; re-extraction from archive doesn't need HTTP
- Uses `pypdf.PdfReader(io.BytesIO(pdf_bytes))` to read, iterates pages, joins text with double newlines
- Raises `ExtractionError` on: invalid PDF (stage="read"), no extractable text (stage="extract"), empty result (stage="extract")
- Created `tests/fixtures/extraction/sample.pdf` (valid PDF, ~312 chars extractable) and `blank.pdf` (blank page, no text)
- Created `tests/unit/test_pdf_extraction.py` with 9 tests: success path (3), plain text output (2), failure paths (4)
- Files changed: `src/influx/extraction/pdf.py`, `tests/fixtures/extraction/sample.pdf`, `tests/fixtures/extraction/blank.pdf`, `tests/unit/test_pdf_extraction.py`
- **Learnings for future iterations:**
  - pypdf accepts bytes via `PdfReader(io.BytesIO(pdf_bytes))` — no temp files needed
  - `page.extract_text()` returns empty string (not None) for pages without text content
  - Design choice: pdf.py takes bytes, not URLs, so it works for both HTTP-fetched PDFs and locally-stored archive files
  - Minimal valid PDFs can be created with raw PDF syntax for fixtures (hand-crafted, no reportlab needed)
  - Full test suite is 901 tests after US-001…US-007, all passing
---

## 2026-04-25 - US-008
- Created `src/influx/extraction/article.py` with `extract_article()` function for generic web article extraction
- `extract_article()` reuses `_strip_tags` and `_clean_html_fragments` from `extraction/html.py`, uses `min_web_chars` (default 500) instead of `min_html_chars`
- Created 3 HTML test fixtures in `tests/fixtures/extraction/`: web_article, short_web_article, web_with_script
- Created `tests/unit/test_article_extraction.py` with 12 tests covering success path, min-length rejection, tag-stripping for all 4 default tags, no-HTML-fragments, and failure propagation
- Files changed: `src/influx/extraction/article.py`, `tests/fixtures/extraction/*.html` (3 files), `tests/unit/test_article_extraction.py`
- **Learnings for future iterations:**
  - `article.py` shares tag-stripping and fragment cleanup with `html.py` via imports — DRY across extraction modules
  - The only difference from `html.py` is: different result type (`ArticleExtractionResult` with `source="article"`), different min-length parameter name (`min_web_chars` vs `min_html_chars`), and lower default threshold (500 vs 1000)
  - Full test suite is 913 tests after US-001…US-008, all passing
---

## 2026-04-25 - US-009
- Replaced PRD 04 stub in `src/influx/enrich.py` with real LLM-backed `tier1_enrich` implementation
- `tier1_enrich(title, abstract, profile_summary, config)` renders `prompts.tier1_enrich`, calls `models.enrich` via `_call_json_model`, validates against `Tier1Enrichment`
- Added `_call_json_model(config, slot_name, prompt)` — reusable JSON-mode inference helper resolving provider/model from runtime config via OpenAI-compatible `/chat/completions` endpoint
- Validation/transport failures surface as `LCMAError` with appropriate `stage` and `model` fields (FR-ENR-6)
- Removed `Tier1Result` dataclass and stub implementation
- Replaced 7 stub tests with 12 tests: success (3), validation failure (4 including AC-07-A `contributions: []`), prompt rendering (2), model slot verification (2), transport failure (1)
- Files changed: `src/influx/enrich.py`, `tests/unit/test_enrich.py`
- **Learnings for future iterations:**
  - `_call_json_model` is a generic helper reusable for US-010 (tier3_extract) — same pattern, different slot name and schema
  - Patch `influx.enrich._call_json_model` in tests to inject fake responses; verify slot name and config via side_effect capture
  - The conftest `influx_config_env` fixture provides all three model slots (filter, enrich, extract) and all three prompt entries
  - `LCMAError(message, model=..., stage=..., detail=...)` is the standard error shape for enrichment/extraction failures
  - Full test suite is 918 tests after US-001…US-009, all passing
---

## 2026-04-25 - US-010
- Added `tier3_extract(title, full_text, config)` to `src/influx/enrich.py` — renders `prompts.tier3_extract`, calls `models.extract` via `_call_json_model`, validates against `Tier3Extraction`
- Validation failure (e.g. `claims: []`) surfaces as `LCMAError(stage="validate", model="extract")` per FR-ENR-6
- Oversize strings (> 500 chars) are truncated by the `Tier3Extraction` schema's `field_validator`, not by `tier3_extract` itself
- Added 13 tests to `tests/unit/test_enrich.py`: success (3), truncation (2), validation failure (3), prompt rendering (2), model slot (2), transport failure (1)
- Files changed: `src/influx/enrich.py`, `tests/unit/test_enrich.py`
- **Learnings for future iterations:**
  - `tier3_extract` follows the exact same pattern as `tier1_enrich` — different slot name (`"extract"` vs `"enrich"`), different schema, different prompt variables
  - The `_call_json_model` helper is fully reusable across both callers — no changes needed
  - `Tier3Extraction` schema handles truncation transparently via `field_validator(mode="before")`, so `tier3_extract` just passes raw model output to `model_validate`
  - Full test suite is 931 tests after US-001…US-010, all passing
---

## 2026-04-25 - US-011
- Added optional `full_text: str | None = None` parameter to `render_note()` in `src/influx/notes.py`
- When provided, emits `## Full Text` section between `## Summary` and `## Profile Relevance`; omitted entirely when `None` or empty (FR-ENR-6)
- Created `tests/unit/test_full_text_render.py` with 10 tests: 3 rendered, 4 omitted, 3 ordering
- Files changed: `src/influx/notes.py`, `tests/unit/test_full_text_render.py`
- **Learnings for future iterations:**
  - `render_note` uses keyword-only arguments — new optional params can be added without breaking callers
  - The canonical section order is: Archive → Summary → Full Text → (Tier 3 sections) → Profile Relevance → User Notes
  - `if full_text:` handles both `None` and `""` — falsy check is the simplest gate for optional sections
  - Full test suite is 941 tests after US-001…US-011, all passing
---

## 2026-04-25 - US-012
- Added optional `tier3_extraction: Tier3Extraction | None = None` parameter to `render_note()` in `src/influx/notes.py`
- When provided, emits `## Claims`, `## Datasets & Benchmarks`, `## Builds On`, `## Open Questions` sections (one bullet per item) between `## Full Text` and `## Profile Relevance`
- `potential_connections` is NOT rendered (consumed by PRD 08 LCMA only)
- When `None`, all four sections are omitted entirely (FR-ENR-6)
- Created `tests/unit/test_tier3_render.py` with 18 tests: 9 rendered, 3 omitted, 4 ordering, 2 potential_connections not rendered
- Files changed: `src/influx/notes.py`, `tests/unit/test_tier3_render.py`
- **Learnings for future iterations:**
  - `Tier3Extraction` imported from `influx.schemas` — notes.py now depends on schemas.py
  - Tier 3 sections render even with empty optional lists (e.g. `datasets=[]`) — headings without bullets
  - Ruff catches f-strings without placeholders (F541) — use plain strings for static text
  - Full test suite is 959 tests after US-001…US-012, all passing
---

## 2026-04-25 - US-013
- Added optional `tier1_enrichment: Tier1Enrichment | None = None` parameter to `render_note()` in `src/influx/notes.py`
- When provided, renders structured `## Summary` with `### Contributions` (bullet list), `### Method`, `### Result`, `### Relevance` sub-blocks
- When absent and no plain `summary` text, omits `## Summary` entirely (FR-ENR-6, AC-07-A)
- Backward compatible: existing callers passing `summary=...` still get plain-text `## Summary`
- When `tier1_enrichment` is provided, it overrides the plain `summary` parameter
- Created `tests/unit/test_summary_render.py` with 18 tests: 8 rendered, 3 omitted, 4 ordering, 3 backward compat
- Files changed: `src/influx/notes.py`, `tests/unit/test_summary_render.py`
- **Learnings for future iterations:**
  - `tier1_enrichment` takes priority over `summary` param — structured rendering overrides plain text
  - For "section omitted" tests, pass `summary=""` (empty) + `tier1_enrichment=None`
  - Existing golden-file and roundtrip tests pass unchanged since they use the `summary=...` param path
  - Full test suite is 977 tests after US-001…US-013, all passing
---

## 2026-04-25 - US-014
- Created `src/influx/extraction/pipeline.py` with `extract_arxiv_text()` — HTML→PDF→abstract-only cascade for arXiv papers
- Added `build_arxiv_note_item()` to `src/influx/sources/arxiv.py` — builds complete ProfileItem with extraction, tier tags, full-text section, and repair-needed logic
- Extraction cascade: tries HTML (`arxiv.org/html/{id}`) first, falls back to PDF (`arxiv.org/pdf/{id}.pdf`), raises ExtractionError when both fail
- Tag logic: `text:html`/`text:pdf`/`text:abstract-only` based on extraction outcome; `full-text` iff extraction succeeded AND score >= full_text threshold; `influx:repair-needed` when extraction fails above threshold; NO `influx:text-terminal` on initial write (AC-M2-3)
- Below full_text threshold: extraction not attempted, `text:abstract-only` set, no `full-text` tag, no `## Full Text` section
- Created `tests/unit/test_extraction_pipeline.py` with 9 tests (3 HTML success, 3 PDF fallback, 3 both-fail)
- Created `tests/unit/test_build_arxiv_note_item.py` with 18 tests (4 HTML success, 1 PDF fallback, 4 both-fail, 2 below-threshold, 7 item shape/metadata)
- Files changed: `src/influx/extraction/pipeline.py` (new), `src/influx/sources/arxiv.py`, `tests/unit/test_extraction_pipeline.py` (new), `tests/unit/test_build_arxiv_note_item.py` (new)
- **Learnings for future iterations:**
  - `extract_arxiv_text` catches both `ExtractionError` and `NetworkError` from each tier, allowing clean fallthrough
  - `build_arxiv_note_item` uses `ProfileThresholds` from config; explicit `thresholds` param overrides for testing
  - `render_note` requires `ingested-by:influx` in tags — must be included when building items
  - Mock extraction in build_arxiv_note_item tests via `@patch("influx.sources.arxiv.extract_arxiv_text")` — patch at the import site, not the definition site
  - Full test suite is 1004 tests after US-001…US-014, all passing
---

## 2026-04-25 - US-015
- Wired `tier1_enrich` and `tier3_extract` into `build_arxiv_note_item` in `src/influx/sources/arxiv.py`
- Tier 1: called when score >= relevance threshold; uses `profile.description` as `profile_summary`; on `LCMAError`, suppresses `## Summary` and sets `influx:repair-needed`
- Tier 3: called when score >= deep_extract AND extracted_text is available; on `LCMAError`, no Tier 3 sections and `influx:repair-needed`
- Per-tier failure independence: each enrichment try/except is independent; extraction/Tier1/Tier3 can each fail without affecting others
- Tag derivation: `influx:deep-extracted` iff tier3_result is not None; `influx:repair-needed` set exactly once even with multiple failures
- Summary suppression: when Tier 1 attempted but failed, `summary_text=""` suppresses plain-text `## Summary` (AC-07-A)
- Updated 2 existing US-014 tests to set enrichment thresholds high (isolation from enrichment behavior)
- Added 23 new unit tests: 5 Tier1 success, 3 Tier1 failure, 5 Tier3 success, 3 Tier3 failure, 3 per-tier independence, 4 tag derivation invariants
- Files changed: `src/influx/sources/arxiv.py`, `tests/unit/test_build_arxiv_note_item.py`
- **Learnings for future iterations:**
  - Existing tests with scores >= relevance need enrichment mocks OR high enrichment thresholds to isolate extraction-only behavior
  - `LCMAError` is raised by `_call_json_model` when model slot is not configured — tests without model config will trigger this
  - `profile_cfg.description` serves as `profile_summary` for tier1_enrich — already available from config lookup
  - When tier1 is attempted but fails, must suppress plain summary too (`summary_text=""`) or `## Summary` renders from abstract
  - `tier3_extract` requires `extracted_text` — not called when extraction itself failed (no full text available)
  - Full test suite is 1027 tests after US-001…US-015, all passing
---

## US-016 — Wire production-default repair hooks (re_extract_archive, tier2_enrich, tier3_extract)
- Created `src/influx/repair_hooks.py` with `make_default_sweep_hooks(config)` factory
- Three hook factories using closure pattern: `_make_re_extract_archive_hook`, `_make_tier2_enrich_hook`, `_make_tier3_extract_hook`
- Content manipulation helpers: `_insert_full_text_section`, `_insert_tier3_sections`, `_render_tier3_sections`, `_extract_full_text_body`, `_extract_title`
- Archive file reading: `_read_archive_file` + `_extract_from_archive` (PDF via `extract_pdf`, HTML via `trafilatura` + `_strip_tags`/`_clean_html_fragments`)
- Modified `src/influx/repair.py` sweep() to use production defaults when `hooks=None` (lazy import to avoid circular deps)
- Updated `tests/unit/test_repair_sweep.py` to pass `SweepHooks()` explicitly (preserves PRD 06 test isolation)
- Created `tests/unit/test_repair_hooks_production.py` with 31 tests: factory, re_extract (PDF/HTML/outcomes), tier2 (success/failure), tier3 (success/failure), content helpers, injection seam
- Files changed: `src/influx/repair_hooks.py` (new), `src/influx/repair.py`, `tests/unit/test_repair_hooks_production.py` (new), `tests/unit/test_repair_sweep.py`
- **Learnings for future iterations:**
  - Factory return types must match Protocol types (e.g., `-> ReExtractArchiveHook`), not the result type
  - `note.get("tags", [])` returns `object` when `note: dict[str, object]` — need `isinstance` guard for pyright
  - `SweepHooks()` with no args gives all-None hooks — useful for test isolation from production defaults
  - Full test suite is 1058 tests after US-001…US-016, all passing
---

## 2026-04-25 - US-017
- Created `tests/integration/test_arxiv_html_to_full_text.py` with 7 tests covering AC-M2-1
- Tests mock `guarded_fetch` at the HTTP layer and exercise the real extraction pipeline (tag-stripping → trafilatura → pipeline.py → build_arxiv_note_item → render_note)
- Asserts: `text:html` tag, `full-text` tag, `## Full Text` section populated with extracted content >= 1000 chars, no HTML fragments, no `influx:repair-needed`, correct HTML URL fetched
- Files changed: `tests/integration/test_arxiv_html_to_full_text.py` (new)
- **Learnings for future iterations:**
  - Integration tests mock `influx.extraction.html.guarded_fetch` (the import site in html.py) — this lets the real extraction pipeline run while controlling HTTP responses
  - The HTML fixture needs to be substantial enough for trafilatura to extract >= 1000 chars — short HTML snippets may get below the threshold after extraction
  - Set enrichment thresholds high (relevance=100, deep_extract=100) to isolate extraction-only integration tests from enrichment behavior
  - Full test suite is 1065 tests after US-001…US-017, all passing
---
