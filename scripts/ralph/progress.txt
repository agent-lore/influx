# Ralph Progress Log
Started: Sun Apr 26 17:50:45 UTC 2026

## Codebase Patterns
- OTEL package imports: use `opentelemetry.sdk.trace` and `opentelemetry.trace` (not `opentelemetry.api` or `opentelemetry.sdk`)
- No `InMemorySpanExporter` in the installed OTEL SDK version — use a custom `_CollectingExporter(SpanExporter)` for test span capture
- Config already has `TelemetryConfig` model and env overrides for `INFLUX_OTEL_ENABLED` and `INFLUX_OTEL_CONSOLE_FALLBACK` in `config.py`
- Test fixtures: use `influx_config_env` from `tests/conftest.py` for config-based tests; `fake_lithos_sse_url` for integration tests needing a reachable Lithos endpoint
- Import-stubbing pattern for simulating absent packages: save `sys.modules` keys, monkeypatch-delete them, override `builtins.__import__`, restore in finally block
- Service-level tests: use `FastAPI` + `TestClient` for boot/serve assertions; `run_profile()` for run lifecycle
- Telemetry singleton: use `get_tracer(force_rebuild=True)` in tests to pick up env changes
- OTEL global TracerProvider: do NOT use `trace.set_tracer_provider()` — use `provider.get_tracer()` from a local `TracerProvider` to avoid "Overriding not allowed" warnings
- Console fallback test pattern: use `ConsoleSpanExporter(out=io.StringIO())` instead of capsys/capfd
- `_make_collecting_tracer()` helper in test_telemetry.py: returns `(InfluxTracer, collected_spans_list)` for span assertions
- `run_profile()` delegates to `_run_profile_body()` — use `patch("influx.scheduler._run_profile_body")` to test span wiring without Lithos
- `current_run_id` ContextVar in `telemetry.py` — set by `run_profile()`, read by downstream span sites (filter, fetchers) for `influx.run_id` attribute
- `ArxivItem` fields: `arxiv_id`, `title`, `abstract`, `published` (datetime), `categories` — no `authors`, `updated`, `pdf_url`
- `fetch_arxiv` is synchronous — mock with `MagicMock`, not `AsyncMock`
- Fetch/storage spans belong at the provider level (inside `make_*_item_provider`) where `profile` is available, not in raw fetch functions

---

## 2026-04-26 - US-001
- Implemented OTEL optional dependency group and no-op telemetry wrapper module
- Files changed:
  - `pyproject.toml` — added `[project.optional-dependencies] otel = [...]`
  - `src/influx/telemetry.py` — new file: `InfluxTracer`, `SpanWrapper`, `get_tracer()` with no-op path
  - `tests/unit/test_telemetry.py` — 13 unit tests covering all 5 AC scenarios
  - `tests/integration/test_otel_absent_service.py` — AC-M4-3 service-level test (boot + serve + run)
- **Learnings for future iterations:**
  - The OTEL SDK version in this project does not have `InMemorySpanExporter` — use a custom collecting exporter pattern instead
  - `_otel_packages_available()` must check `opentelemetry.sdk.trace` and `opentelemetry.trace` (not `.api` / `.sdk`)
  - The telemetry wrapper uses a module-level singleton rebuilt via `get_tracer(force_rebuild=True)` — future stories (US-003..US-007) should use this to wire spans into call sites
  - ruff requires isort-compatible import ordering — `from collections.abc` before `from unittest.mock`
---

## 2026-04-26 - US-002
- Implemented telemetry wrapper enabled path: span creation, attributes, and console fallback exporter
- Files changed:
  - `src/influx/telemetry.py` — added `_console_fallback_enabled()`, `_otlp_endpoint_configured()`, console fallback exporter wiring in `_build_tracer()`, switched to local `TracerProvider` (no global provider override)
  - `tests/unit/test_telemetry.py` — 13 new tests (26 total): enabled span creation, attribute setting (individual + batch + merge), console fallback, AC-10-A regression guard; also fixed pre-existing pyright errors in `_blocked_import` type annotations
- **Learnings for future iterations:**
  - OTEL global `TracerProvider` cannot be overridden once set (`trace.set_tracer_provider()` logs warning and is silently ignored). Use `provider.get_tracer()` directly from a local `TracerProvider` instead of the global `trace.get_tracer()`
  - `ConsoleSpanExporter` output cannot be reliably captured with `capsys` or `capfd` in pytest — use `ConsoleSpanExporter(out=io.StringIO())` to capture output directly in tests
  - Console fallback condition: `INFLUX_OTEL_CONSOLE_FALLBACK=true` AND `OTEL_EXPORTER_OTLP_ENDPOINT` is unset/empty
  - `_make_collecting_tracer()` test helper pattern: creates a local `TracerProvider` + `_CollectingExporter` + `InfluxTracer` for span assertions without global state
---

## 2026-04-26 - US-003
- Wired telemetry spans into run orchestration (`influx.run`) and filter call site (`influx.filter`)
- Files changed:
  - `src/influx/telemetry.py` — added `current_run_id` ContextVar for threading run_id to downstream span sites
  - `src/influx/scheduler.py` — extracted `_run_profile_body()` from `run_profile()`, wrapped body in `influx.run` span with `influx.profile`, `influx.run_id`, `influx.run_type` attributes; added `uuid` and `telemetry` imports
  - `src/influx/sources/arxiv.py` — wrapped `filter_scorer()` call in `influx.filter` span with `influx.profile`, `influx.run_id`, `influx.item_count` attributes
  - `tests/unit/test_telemetry_wiring.py` — 7 new tests: 3 for `influx.run` span (scheduled/manual/backfill run types), 2 for `influx.filter` span (direct + via provider), 2 for OTEL-disabled regression (AC-10-A)
- **Learnings for future iterations:**
  - `run_profile()` body was extracted to `_run_profile_body()` to avoid re-indenting 300+ lines when wrapping with a `with tracer.span(...)` block. Future span wiring at other call sites can follow the same pattern.
  - `current_run_id` ContextVar pattern: `run_profile()` sets it before calling the provider; downstream sites (filter scorer in arxiv.py) read it via `current_run_id.get()`. This avoids changing function signatures to thread `run_id` through.
  - When testing `run_profile()` span wiring, patch `_run_profile_body` as `AsyncMock(return_value=None)` to skip Lithos interactions.
  - `fetch_arxiv` is synchronous — always use `MagicMock`, not `AsyncMock`, when mocking it.
  - `ArxivItem` is a frozen dataclass with only 5 fields: `arxiv_id`, `title`, `abstract`, `published` (datetime), `categories`.
---

## 2026-04-26 - US-004
- Wired telemetry spans into source fetchers: `influx.fetch.arxiv` and `influx.fetch.rss`
- Files changed:
  - `src/influx/sources/arxiv.py` — wrapped fetch+cache block in `make_arxiv_item_provider` with `influx.fetch.arxiv` span; sets `influx.item_count` after fetch completes
  - `src/influx/sources/rss.py` — added telemetry imports; wrapped RSS feed loop in `make_rss_item_provider` with `influx.fetch.rss` span; sets `influx.item_count` after all feeds processed
  - `tests/unit/test_telemetry_wiring.py` — 4 new tests (11 total): fetch.arxiv span + attributes, fetch.rss span + attributes, OTEL-disabled regression for both
- **Learnings for future iterations:**
  - Fetch spans are best placed at the provider level (inside `make_*_item_provider`) where `profile` is available, not inside the raw `fetch_arxiv()`/`_fetch_rss_feed()` functions which lack profile context
  - `influx.item_count` must be set after the fetch completes (via `fetch_span.set_attribute()`) since the count isn't known at span creation
  - RSS imports must be in alphabetical order by module path — `influx.telemetry` sorts after `influx.storage`
  - `_fetch_rss_feed` is async — use `AsyncMock` when patching it in tests
---
