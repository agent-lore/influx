# Ralph Progress Log
Started: Sun Apr 26 11:45:13 UTC 2026
---

## Codebase Patterns
- Use `feedparser.parse(content)` for RSS/Atom parsing; access entry fields via `str(entry.get("field", ""))` to satisfy pyright
- `RssSourceEntry` has Literal type for `source_tag` — test helpers must use `Literal["rss", "blog"]` not bare `str`
- Frozen dataclasses with `slots=True` for immutable records (matches arXiv pattern)
- Test fixtures live under `tests/fixtures/<source>/*.xml`
- Config schema already has `RssSourceEntry` and `ProfileSources.rss` from prior PRDs
- `slugify_feed_name()` in `src/influx/slugs.py` for FR-ST-2 slug generation
- `url_hash(source_url)` in `src/influx/urls.py` returns 10-char hex SHA-256 of normalised URL for archive filename disambiguation
- RSS archive `item_id` for `build_archive_path()` = `f"{feed_slug}-{YYYY-MM-DD}-{url_hash}"`
- `download_archive()` in `storage.py` accepts `expected_content_type` param (default `"pdf"`, pass `"html"` for RSS)
- `build_rss_note_item()` in `sources/rss.py` builds complete `ProfileItem` dict with archive download + article extraction (fallback to feed summary) + note rendering
- RSS note path = `articles/{source_tag}/{YYYY}/{MM}`, tags include `source:{source_tag}`, archive under `{source_tag}/{YYYY}/{MM}/`
- Integration tests for RSS use `FakeArticleServer` (simple `http.server` serving HTML) with `SecurityConfig(allow_private_ips=True)`

---

## 2026-04-26 - US-001
- What was implemented: RSS/Atom parser module with feedparser, test fixtures, and unit tests
- Files changed:
  - `pyproject.toml` — added `feedparser>=6.0,<7` dependency
  - `src/influx/sources/rss.py` — new module: `RssFeedItem` dataclass + `parse_feed()` function
  - `tests/fixtures/rss/sample_atom.xml` — Atom feed fixture with 2 entries
  - `tests/fixtures/rss/sample_rss2.xml` — RSS 2.0 feed fixture with 2 entries
  - `tests/unit/test_rss_fetcher.py` — 9 tests covering Atom parsing, RSS 2.0 parsing, source_tag passthrough, feed_name, and no cross-bucket leakage
- **Learnings for future iterations:**
  - feedparser's `.get()` returns typed unions that pyright checks strictly — always wrap with `str()` for string fields
  - RssSourceEntry.source_tag is `Literal["rss", "blog"]` not `str` — test helpers need matching type annotations
  - The existing config schema already validates RSS feeds; no config changes needed for the parser itself
---

## 2026-04-26 - US-002
- What was implemented: `url_hash()` function in `src/influx/urls.py` — computes first 10 hex chars of SHA-256 over `normalise_url(source_url)` for archive filename disambiguation
- Files changed:
  - `src/influx/urls.py` — added `url_hash()` function and `_URL_HASH_LEN` constant
  - `tests/unit/test_archive_url_hash.py` — 8 tests: determinism (same URL twice, equivalent URLs, trailing slash), collision (different paths, same-feed-same-date, different hosts, different query params), hex format validation
- **Learnings for future iterations:**
  - `url_hash()` normalises via `normalise_url()` before hashing, so tracking params and case differences don't affect the hash
  - `build_archive_path()` in `storage.py` takes `item_id` as a string — for RSS, compose it as `f"{feed_slug}-{YYYY-MM-DD}-{url_hash}"` before passing
  - ruff enforces import ordering: `hashlib` (stdlib) must come after `from __future__` with no blank line separating stdlib imports from each other
---

## 2026-04-26 - US-003
- What was implemented: RSS archive layout and note-path integration end-to-end
- Files changed:
  - `src/influx/storage.py` — added `expected_content_type` param to `download_archive()` (typed as `ContentTypeFamily`, default `"pdf"`)
  - `src/influx/sources/rss.py` — added `build_rss_note_item()` function: downloads article HTML via guarded HTTP client, archives on disk at PRD 09 layout, renders canonical note with correct `source:*` tag and storage path
  - `tests/fixtures/rss/collision_atom.xml` — fixture with two items on same date for collision testing
  - `tests/integration/test_rss_to_lithos.py` — 11 integration tests covering: source tag agreement (rss/blog), archive layout on disk, collision (two items same date → distinct files), determinism (same URL → same hash), end-to-end from Atom and RSS 2.0 fixtures, SSRF guard verification
- **Learnings for future iterations:**
  - `download_archive()` `expected_content_type` must use `ContentTypeFamily` Literal type (from `http_client.py`), not bare `str` — pyright will reject `str`
  - For integration tests with RSS article downloads, use a `FakeArticleServer` (simple `http.server.HTTPServer` with `_ArticleHandler`) and `SecurityConfig(allow_private_ips=True)`
  - RSS archive path layout: `{archive_root}/{source_tag}/{YYYY}/{MM}/{feed-slug}-{YYYY-MM-DD}-{url-hash}.html`
  - To verify SSRF guard works, test with `allow_private_ips=False` — download to localhost will fail → `influx:archive-missing` tag
---

## 2026-04-26 - US-004
- What was implemented: RSS web extraction fallback to feed summary when article body is below `extraction.min_web_chars` (FR-ENR-3, AC-09-J)
- Files changed:
  - `src/influx/sources/rss.py` — added `extract_article()` call after archive download; catches `ExtractionError`/`NetworkError` and falls back to `item.summary`; extracted text used as summary when extraction succeeds
  - `tests/integration/test_rss_to_lithos.py` — added `TestExtractionFallbackToSummary` class with 2 tests: (1) short article body → feed summary used, (2) long article body → extracted text used
- **Learnings for future iterations:**
  - `extract_article()` in `extraction/article.py` does its own `guarded_fetch()` — separate from `download_archive()` — so there are two HTTP requests per RSS item (one for archive, one for extraction)
  - `trafilatura.extract()` needs substantial HTML content (real `<article>` tags, multiple `<p>` blocks) to produce text >= `min_web_chars`; minimal test HTML will always fall back
  - Integration tests for extraction fallback use separate `_ShortArticleHandler` and `_LongArticleHandler` HTTP handlers to control the extraction outcome
  - The fallback is transparent: `abstract_or_summary` in the returned dict reflects whichever source was used (extracted text or feed summary)
---
