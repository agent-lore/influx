## Codebase Patterns
- Use `asyncio.Lock` per profile for coordinator — safe on single event loop, no race between check-and-acquire
- Dev dependencies (ruff, pyright, pytest-asyncio, respx) need explicit pip install in fresh envs — not auto-installed from dependency-groups
- Ruff enforces import sorting (I001) and combined `with` statements (SIM117) — run `ruff check --fix` after creating new files
- Test fixture `influx_config_env` in conftest.py provides realistic v0.7 TOML + TEST_PROVIDER_API_KEY env var
- pytest-asyncio configured with `asyncio_mode = "auto"` — no need for `@pytest.mark.asyncio` decorators (but they don't hurt)

# Ralph Progress Log
Started: Fri Apr 24 14:02:16 UTC 2026
---

## 2026-04-24 - US-001
- Implemented per-profile execution coordinator with asyncio.Lock-based locking
- Added fastapi>=0.115, uvicorn[standard]>=0.34, apscheduler>=3.10 to pyproject.toml
- Added pytest-asyncio>=0.24 to dev dependencies, configured asyncio_mode = "auto"
- Files changed:
  - pyproject.toml (dependencies + pytest-asyncio + asyncio_mode config)
  - src/influx/coordinator.py (new: Coordinator, ProfileBusyError, RunKind)
  - tests/unit/test_coordinator.py (new: 12 tests covering same-profile reject, cross-profile allow, lifecycle)
- **Learnings for future iterations:**
  - `asyncio.Lock` has no `try_lock()` — use `locked()` check + immediate `acquire()` (safe on single event loop)
  - `ProfileBusyError` carries `.profile` attribute for HTTP layer to build 409 response
  - `Coordinator.hold()` context manager is the preferred API for acquire-or-raise + auto-release
  - All 164 tests pass (12 new coordinator + 152 existing)
---
