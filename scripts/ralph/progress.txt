# Ralph Progress Log
Started: Fri Apr 24 11:54:09 UTC 2026

## Codebase Patterns
- Error subclasses use keyword-only args for structured context fields (url, kind, reason, etc.)
- Tests use class-based grouping (TestXxx) with pytest; AAA pattern
- Quality checks: `uv run pytest tests/`, `uv run pyright <file>`, `uv run ruff check <file>`
- Config model is in config.py with pydantic v2 BaseModel classes
- CLI dispatcher is in main.py with argparse subcommands
- HTTP client uses respx for mocking httpx requests in tests
- SSRF guard patches `socket.getaddrinfo` via `_fake_getaddrinfo(ip)` helper for controlled IP resolution in tests
- Ruff line length is 88 chars; use `uv run ruff check --fix` for auto-fixable issues like import sorting
- Streaming uses `client.stream("GET", url)` + `response.iter_bytes()` with combined `with` statement (ruff SIM117)
- For timeout tests, patch `influx.http_client.httpx.Client` and set `ctx.stream.side_effect` to httpx timeout exceptions
- Redirect re-validation: use `follow_redirects=False` + manual for-loop with `_validate_scheme`/`_ssrf_check` at each hop
- For multi-host redirect tests, use `_multi_resolve({"host": "ip", ...})` helper instead of `_fake_getaddrinfo`

---

## 2026-04-24 - US-001
- Implemented NetworkError, LithosError, LCMAError, ExtractionError in src/influx/errors.py
- NetworkError: url (str), kind (str), reason (str, default "")
- LithosError: operation (str, default ""), status_code (int|None, default None), detail (str, default "")
- LCMAError: model (str, default ""), stage (str, default ""), detail (str, default "")
- ExtractionError: url (str, default ""), stage (str, default ""), detail (str, default "")
- Created tests/unit/test_errors.py with 16 tests covering isinstance chains, structured context, defaults, and parametrized catchability
- Files changed: src/influx/errors.py, tests/unit/test_errors.py
- **Learnings for future iterations:**
  - All error subclasses use keyword-only constructor args for structured fields
  - NetworkError requires url and kind (no defaults); other subclasses have all-optional context fields
  - Full test suite is 100 tests and runs in <0.3s
---

## 2026-04-24 - US-002
- Added httpx>=0.27 to project dependencies and respx>=0.22 to dev dependencies
- Created src/influx/http_client.py with guarded_fetch() returning FetchResult dataclass (body, status_code, content_type, final_url)
- Implemented scheme allow-list (http/https only) raising NetworkError(kind="scheme")
- Implemented SSRF IP-classification guard: resolves hostname via socket.getaddrinfo, classifies IPs with ipaddress module (is_loopback, is_link_local, is_private, is_multicast)
- SSRF guard raises NetworkError(kind="ssrf") with offending URL and resolved IP in reason
- allow_private_ips=True bypasses SSRF rejection
- DNS resolution failure raises NetworkError(kind="dns")
- Created tests/unit/test_http_client.py with 17 tests covering scheme rejection, SSRF for all 4 IP classes, allow-through path, FetchResult attributes, and DNS failure
- Files changed: pyproject.toml, uv.lock, src/influx/http_client.py, tests/unit/test_http_client.py
- **Learnings for future iterations:**
  - respx mocks httpx at the transport level; use `@respx.mock` decorator + `respx.get(url).mock(return_value=...)`
  - For SSRF tests, patch `influx.http_client.socket.getaddrinfo` not `socket.getaddrinfo` directly
  - FetchResult is a frozen dataclass with slots for immutability
  - Future stories (US-003/004/005) will extend guarded_fetch with streaming, content-type check, and redirect re-validation
---

## 2026-04-24 - US-003
- Switched guarded_fetch from buffered `response.content` to streaming `response.iter_bytes()` via `client.stream("GET", url)`
- Added mid-stream abort: raises NetworkError(kind="oversize") when cumulative body exceeds max_download_bytes; partial body is not returned
- Connect and read timeouts already bounded by timeout_seconds via httpx.Timeout; timeout raises NetworkError(kind="timeout")
- Added 6 new tests: oversize rejection, no-body-returned assertion, exact-limit success, under-limit success, connect timeout, read timeout
- Files changed: src/influx/http_client.py, tests/unit/test_http_client.py
- **Learnings for future iterations:**
  - respx handles streaming transparently; `httpx.Response` with `content=` works with `iter_bytes()` in tests
  - Use combined `with` for nested context managers to satisfy ruff SIM117
  - NetworkError re-raise inside try/except must come before httpx exception handlers to avoid catching it as HTTPError
  - Full test suite is now 123 tests
---

## 2026-04-24 - US-004
- Added ContentTypeFamily literal type ("html", "pdf", "xml") and MIME mapping to http_client.py
- Added _check_content_type() helper that extracts MIME from content-type header (stripping charset params) and validates against family
- Added expected_content_type parameter to guarded_fetch (optional, None = skip check)
- Raises NetworkError(kind="content_type_mismatch") on mismatch with reason containing both expected and actual MIME
- Added 12 new tests: 8 positive cases (HTML variants, PDF, XML/Atom/RSS), 1 None-skip case, 3 mismatch cases
- Files changed: src/influx/http_client.py, tests/unit/test_http_client.py
- **Learnings for future iterations:**
  - Content-type header parsing: split on ";" first to strip charset/boundary params before matching
  - ContentTypeFamily is exported from http_client module for type-safe callers
  - Full test suite is now 135 tests
---

## 2026-04-24 - US-005
- Switched guarded_fetch from `follow_redirects=True` to manual redirect loop with `follow_redirects=False`
- Every redirect hop re-validates scheme (http/https allow-list) and SSRF IP classification via `_validate_scheme` and `_ssrf_check`
- Added `_MAX_REDIRECTS = 20` constant and `urljoin` for resolving relative Location headers
- Added `for/else` pattern: exceeding max redirects raises NetworkError(kind="network")
- `allow_private_ips=True` is honoured consistently at every redirect hop
- Added 4 new tests: redirect-to-private-IP, redirect-to-loopback, redirect-to-bad-scheme, redirect-allows-private-with-flag
- Added `_multi_resolve(mapping)` test helper for multi-host DNS resolution
- Files changed: src/influx/http_client.py, tests/unit/test_http_client.py
- **Learnings for future iterations:**
  - httpx `response.is_redirect` checks both status code AND Location header presence — safe to access `response.headers["location"]` after
  - `continue` inside `with client.stream(...) as response:` properly exits the context manager, closing the stream
  - respx handles `follow_redirects=False` transparently; mock each URL independently
  - Full test suite is now 139 tests
---
