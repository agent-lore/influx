# Ralph Progress Log
Started: Sat Apr 25 14:42:15 UTC 2026
---

## Codebase Patterns
- LithosClient is an SSE-backed MCP client in `src/influx/lithos_client.py`; exposes `read_note`, `list_notes`, `write_note`, `cache_lookup` etc.
- Contract tests for LithosClient live in `tests/contract/test_lithos_client.py` using a `FakeLithosServer` (FastMCP-based) that records calls and supports response queues
- Config schema uses pydantic v2 models in `src/influx/config.py`; `RepairConfig` already exists with `max_items_per_run = 100`
- Test fixtures in `tests/conftest.py` provide `influx_config_env` (realistic v0.7 TOML) and `fake_lithos_sse_url`
- Use `uv run ruff check`, `uv run ruff format`, `uv run pyright`, `uv run pytest` for quality gates
- Git identity needs explicit config in worktrees: `git config user.email/user.name`
- Repair hook protocols live in `src/influx/repair.py`: `ReExtractArchiveHook`, `Tier2EnrichHook`, `Tier3ExtractHook` — all use `typing.Protocol`
- `ExtractionOutcome` enum (UPGRADE/TERMINAL/TRANSIENT) + `ReExtractionResult` dataclass drive abstract-only re-extraction tag logic
- `sweep(profile, *, client, config)` is the async repair entry point; accepts LithosClient + AppConfig via kwargs
- `list_notes` returns `CallToolResult` whose `.content[0].text` is JSON `{"items": [...]}`; each item has `id`, `title`, etc.
- Unit tests for sweep use `AsyncMock` + `MagicMock` to mock LithosClient (no SSE server needed)
- `select_stages()` accepts extracted `archive_path` and `max_profile_score` as params; caller must parse note content via `parse_note` + `parse_archive_path` + `parse_profile_relevance`
- `StageSelection` frozen dataclass with 5 booleans: `archive_retry`, `text_extraction_retry`, `abstract_only_reextraction`, `tier2_retry`, `tier3_retry`
- `ClearingDecision` frozen dataclass + `compute_clearing()` pure function determine post-stage tag removal for `influx:archive-missing` and `influx:repair-needed`
- Clearing logic: archive-missing iff archive_path stored; repair-needed iff archive_ok AND text_ok AND tier2_ok AND tier3_ok (with terminal exemption waiving Tier 2/3)
- `SweepHooks` frozen dataclass groups optional hooks (`re_extract_archive`, `tier2_enrich`, `tier3_extract`); `sweep()` accepts `hooks=` kwarg
- `_rewrite_sweep_note()` uses `client.call_tool("lithos_write", ...)` directly (not `client.write_note()`) for sweep-specific version_conflict semantics
- `SweepWriteError(LithosError)` signals terminal write failure — unresolved version_conflict or transport failure aborts the sweep
- `ContentTooLargeSkipped(Exception)` signals non-fatal content_too_large on repair path — sweep catches and continues; NOT a LithosError subclass
- Config mock in sweep tests needs `config.profiles = []`; client mock needs `client.call_tool` returning `_make_write_result()`
- `run_profile()` in `scheduler.py` is the central run entry point for scheduled, manual, and backfill paths — all pre-run hooks (sweep, etc.) wire here
- FakeLithosServer FIFO queue: when adding new `lithos_list` callers, prepend empty responses for earlier callers so downstream queued responses arrive correctly
- `ArchiveDownloadHook` protocol: `(note: dict[str, object]) -> str` — returns archive path on success; raises `ExtractionError` on failure
- Integration tests for sweep call `sweep()` directly with real `LithosClient` + `FakeLithosServer`; use `_queue_single_note()` pattern to queue list+read+write responses

---

## 2026-04-25 - US-001
- Added `order_by` and `order` optional parameters to `LithosClient.list_notes()` in `src/influx/lithos_client.py`
- Extended `FakeLithosServer.lithos_list` tool in `tests/contract/test_lithos_client.py` to accept and record `order_by`/`order`
- Added 2 contract tests: `test_order_by_and_order_forwarded` and `test_order_by_omitted_when_none`
- `read_note` already existed and satisfies AC2 (note-read operation)
- Files changed: `src/influx/lithos_client.py`, `tests/contract/test_lithos_client.py`
- **Learnings for future iterations:**
  - `list_notes` wraps `lithos_list` MCP tool; params forwarded conditionally (only when non-None)
  - FakeLithosServer uses response queues (`list_responses`, `write_responses`, etc.) — pop from front
  - All tests pass: 626 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-002
- Added `TestRepairConfigKnob` test class to `tests/unit/test_config.py`
- `test_custom_max_items_per_run_accepted`: sets repair.max_items_per_run=50, validates and reads back
- `test_repair_and_feedback_independently_configurable`: sets repair=50 and feedback=30, asserts they differ
- `RepairConfig` schema already existed from prior work (default 100)
- Files changed: `tests/unit/test_config.py`
- **Learnings for future iterations:**
  - RepairConfig with max_items_per_run=100 was already in place from earlier PRD work
  - Ruff enforces 88-char line limit; docstrings and long assert chains need wrapping
  - All tests pass: 628 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-003
- Created `src/influx/repair.py` with hook protocol definitions
- `ExtractionOutcome` enum: UPGRADE, TERMINAL, TRANSIENT — the three abstract-only re-extraction outcomes
- `ReExtractionResult` frozen dataclass with `outcome` discriminator and optional `upgraded_text_tag`
- `ReExtractArchiveHook` Protocol: `(note, archive_path) -> ReExtractionResult`
- `Tier2EnrichHook` Protocol: `(note) -> None`
- `Tier3ExtractHook` Protocol: `(note) -> None`
- All hooks may raise `ExtractionError` or `LithosError` (documented in Protocol docstrings)
- Created `tests/unit/test_repair_hooks.py` with 15 tests covering: outcome variants, result dataclass, hook substitution for all three hooks, error raising
- Files changed: `src/influx/repair.py` (created), `tests/unit/test_repair_hooks.py` (created)
- **Learnings for future iterations:**
  - Codebase uses `typing.Protocol` for hook interfaces — keeps them decoupled from implementations
  - `ExtractionError` and `LithosError` are in `influx.errors` module; don't import them in repair.py unless used at runtime (ruff F401)
  - Ruff enforces import sorting (I001); use `--fix` to auto-sort
  - All tests pass: 643 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-004
- Added `sweep(profile, *, client, config)` async function to `src/influx/repair.py`
- Calls `lithos_list` with `tags=['influx:repair-needed', f'profile:{profile}']`, `limit=config.repair.max_items_per_run`, `order_by='updated_at'`, `order='asc'`
- Parses JSON response to get items list, iterates in order, re-reads each via `lithos_read`
- Returns empty list cleanly when no candidates (no `lithos_read` calls)
- Created `tests/unit/test_repair_sweep.py` with 9 tests covering: correct list params, zero candidates, iteration order, re-read per candidate, items without id skipped
- Files changed: `src/influx/repair.py`, `tests/unit/test_repair_sweep.py` (created)
- **Learnings for future iterations:**
  - Use `TYPE_CHECKING` guard for `AppConfig`/`LithosClient` imports to avoid circular imports
  - `AsyncMock` + `MagicMock` are the right test pattern for unit-testing sweep (no SSE server)
  - `list_notes` response: `.content[0].text` is JSON with `{"items": [...]}` envelope
  - All tests pass: 652 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-005
- Added `StageSelection` frozen dataclass and `select_stages()` function to `src/influx/repair.py`
- Five stages independently selected: archive_retry, text_extraction_retry, abstract_only_reextraction, tier2_retry, tier3_retry
- `select_stages` takes extracted values (tags, archive_path, max_profile_score, thresholds) — caller parses note content
- `archive_succeeded_this_pass` defaults to False; caller updates after running archive stage
- Files changed: `src/influx/repair.py`
- **Learnings for future iterations:**
  - Stage selection is pure logic — no async, no note parsing, no I/O; easy to unit test
  - `archive_path is not None` is sufficient for "non-empty path: line" check (parse_archive_path never returns empty string)
  - `text:*` tags checked via `any(t.startswith("text:") for t in tags)` — covers text:html, text:pdf, text:abstract-only
  - All tests pass: 652 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-006
- Created `tests/unit/test_repair_stage_selection.py` with 34 tests across 8 test classes
- Covers: archive-missing only, text:abstract-only only, archive-missing + text:abstract-only, high-score Tier 2/3 eligible, influx:text-terminal suppression, stored archive path eligibility, AC-06-A regression, edge cases
- AC-06-A regression: text:html + influx:archive-missing selects archive but NOT text-extraction
- influx:text-terminal suppresses abstract-only, Tier 2, Tier 3 (but NOT archive retry)
- Abstract-only reextraction IS selected when archive path already stored even without archive stage this pass
- Files changed: `tests/unit/test_repair_stage_selection.py` (created)
- **Learnings for future iterations:**
  - `select_stages()` is pure logic (no async, no I/O) — trivial to test directly
  - Use `_select()` helper wrapper to reduce boilerplate across tests
  - Parametrize over text:* variants (`text:html`, `text:pdf`, `text:abstract-only`) to catch edge cases
  - All tests pass: 686 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-008
- Created `tests/unit/test_repair_clearing_rules.py` with 35 tests across 10 test classes
- Covers: archive-missing only; text:abstract-only only; archive-missing + text:abstract-only; high-score deep-extract eligible; influx:text-terminal present
- AC-06-B: archive download + abstract-only Upgrade in one pass clears BOTH influx:archive-missing and influx:repair-needed
- AC-06-C negative: text:abstract-only WITHOUT influx:text-terminal is NOT cleared even when archive + Tier 2 + Tier 3 all succeed
- AC-X-7 high-score-terminal positive: terminal-abstract-only with score >= deep_extract_threshold, archive path stored, no other outstanding tag -> cleared from influx:repair-needed
- Partial repair tests: verify unsatisfied stage-specific tags remain
- Files changed: `tests/unit/test_repair_clearing_rules.py` (created)
- **Learnings for future iterations:**
  - `compute_clearing()` is pure logic (no async, no I/O) — same testability as `select_stages()`
  - Use `_clear()` helper wrapper to reduce boilerplate, same pattern as `_select()` in stage selection tests
  - All tests pass: 721 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-007
- Added `ClearingDecision` frozen dataclass and `compute_clearing()` function to `src/influx/repair.py`
- `ClearingDecision` has two booleans: `clear_archive_missing` and `clear_repair_needed`
- `compute_clearing()` is pure logic (no async, no I/O) — takes post-stage tags + archive_path + score/thresholds
- `influx:archive-missing` cleared iff `archive_path is not None` (FR-NOTE-9)
- `influx:repair-needed` cleared iff ALL: (a) archive_ok, (b) text_ok (html/pdf or abstract-only+terminal), (c) tier2_ok, (d) tier3_ok
- AC-X-7 high-score terminal exemption: `influx:text-terminal` waives Tier 2 and Tier 3 requirements
- AC-06-C: `text:abstract-only` without `influx:text-terminal` naturally fails condition (b), preventing clearing
- Files changed: `src/influx/repair.py`
- **Learnings for future iterations:**
  - `compute_clearing()` mirrors `select_stages()` in design — pure function, same threshold params
  - Terminal exemption is cleanly expressed: `if is_text_terminal: tier2_ok = True` (same for tier3)
  - Text quality check (b) naturally handles AC-06-C without special-casing
  - All tests pass: 686 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-009
- Added `apply_abstract_only_reextraction()` function to `src/influx/repair.py`
- Calls `re_extract_archive` hook and switches on `ExtractionOutcome` discriminator
- UPGRADE: removes `text:abstract-only`, adds the upgraded tag (e.g. `text:html`)
- TERMINAL: keeps `text:abstract-only`, adds `influx:text-terminal`
- TRANSIENT (or ExtractionError): keeps tags unchanged — no `influx:text-terminal`
- `influx:text-terminal` is NEVER set except through Terminal outcome (AC-M2-3)
- Added to `__all__` exports; imported `ExtractionError` at module level
- Files changed: `src/influx/repair.py`
- **Learnings for future iterations:**
  - `apply_abstract_only_reextraction()` is synchronous — hook protocol is sync `__call__`
  - Returns a new tag list (immutable pattern) — caller uses the returned list for further stages
  - ExtractionError catch treats exception as Transient (keeps tags unchanged)
  - All tests pass: 721 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-010
- Created `tests/unit/test_text_terminal.py` with 24 tests across 6 test classes
- Covers all three outcomes: Upgrade (replaces text:abstract-only with text:html/text:pdf, no terminal), Terminal (keeps abstract-only, adds influx:text-terminal), Transient (tags unchanged)
- ExtractionError treated as Transient outcome (tags unchanged, no terminal)
- AC-M2-3 initial-write negative tests: Upgrade, Transient, and ExtractionError never introduce influx:text-terminal; fresh note tags and pre-reextraction tags verified
- Input immutability tests: apply_abstract_only_reextraction() never mutates input tag list
- Files changed: `tests/unit/test_text_terminal.py` (created)
- **Learnings for future iterations:**
  - `apply_abstract_only_reextraction()` is sync; fake hooks are plain functions matching `ReExtractArchiveHook` protocol
  - Use `_apply()` helper wrapper pattern (same as `_select()` and `_clear()` in other test files) to reduce boilerplate
  - Terminal idempotency: function guards against adding duplicate `influx:text-terminal`
  - All tests pass: 745 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-011
- Implemented rewrite-on-every-visit retry-order advancement invariant in `src/influx/repair.py`
- Added `SweepWriteError` exception (subclass of LithosError) for terminal write failures
- Added `SweepHooks` frozen dataclass grouping optional `re_extract_archive`, `tier2_enrich`, `tier3_extract` hooks
- Added `_rewrite_sweep_note()` async helper: writes note via `lithos_write`, handles version_conflict with FR-MCP-7 re-read + re-merge + retry once, raises `SweepWriteError` on unresolved conflict or transport failure
- Added `_process_sweep_note()`: full per-note pipeline — parse note → select stages → execute stages (with hooks) → compute clearing → apply clearing → rewrite
- Added `_get_profile_thresholds()` helper to extract thresholds from config
- Overhauled `sweep()` to accept optional `SweepHooks`, process each visited note through the full pipeline, and abort on `SweepWriteError`
- Updated `tests/unit/test_repair_sweep.py` with 8 new tests across 3 new test classes:
  - `TestSweepRewriteInvariant`: every note triggers write, no-progress still rewrites, rewrite includes note fields
  - `TestSweepVersionConflict`: re-read + retry on conflict, unresolved conflict aborts, no later candidate after abort
  - `TestSweepTransportFailure`: transport failure aborts, no later candidate after abort
- Updated existing test helpers (`_make_config`, `_make_client`) to support `config.profiles` and `client.call_tool`
- Files changed: `src/influx/repair.py`, `tests/unit/test_repair_sweep.py`
- **Learnings for future iterations:**
  - `_rewrite_sweep_note()` calls `client.call_tool("lithos_write", args)` directly (not `client.write_note()`) because sweep needs different error semantics
  - Config mock needs `config.profiles = []` for `_get_profile_thresholds()` fallback defaults
  - Client mock needs `client.call_tool` returning `_make_write_result()` for the lithos_write path
  - `SweepWriteError` inherits from `LithosError` so existing error handlers catch it naturally
  - Note parsing failure in `_process_sweep_note` is caught broadly — note is still rewritten with original tags
  - All tests pass: 753 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-012
- Added `ContentTooLargeSkipped` exception to `src/influx/repair.py` — non-fatal, NOT a subclass of `LithosError`/`SweepWriteError`
- Updated `_rewrite_sweep_note()` to detect `content_too_large` status from lithos_write response and raise `ContentTooLargeSkipped` (both initial write and FR-MCP-7 retry paths)
- Updated `sweep()` to catch `ContentTooLargeSkipped`, log + count via `content_too_large_skipped` counter, and continue to next candidate
- Added 7 unit tests in `tests/unit/test_repair_sweep.py`:
  - `TestSweepContentTooLargeSkipped`: does not abort, untouched note gets one write attempt only, other notes still make progress, multiple oversize notes all skipped
  - `TestContentTooLargeSkippedException`: stores note_id, message contains note_id, is NOT a SweepWriteError
- Files changed: `src/influx/repair.py`, `tests/unit/test_repair_sweep.py`, `scripts/ralph/prd.json`
- **Learnings for future iterations:**
  - `ContentTooLargeSkipped` must NOT inherit from `LithosError` or `SweepWriteError` — sweep catches it separately from terminal failures
  - `_rewrite_sweep_note` returns normally for success but raises for both fatal (`SweepWriteError`) and non-fatal (`ContentTooLargeSkipped`) write outcomes
  - The `content_too_large` status check must come BEFORE the `version_conflict` check in `_rewrite_sweep_note` — a content_too_large is not a version conflict
  - All tests pass: 760 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-013
- Wired `repair.sweep(profile)` into `run_profile()` in `src/influx/scheduler.py`
- Added `from influx.repair import sweep as repair_sweep` import
- Sweep call placed right after `LithosClient` creation, before feedback ingestion step
- Gated on `kind != RunKind.BACKFILL` per FR-REP-2
- `SweepWriteError` propagates through existing `_run_and_release` exception handler in `http_api.py`
- Updated `tests/integration/test_arxiv_to_lithos.py` AC-05-H test to queue an empty list response for the repair sweep's `lithos_list` call
- Updated AC-05-H assertion from 1 to 2 `lithos_list` calls (sweep + feedback)
- Files changed: `src/influx/scheduler.py`, `tests/integration/test_arxiv_to_lithos.py`
- **Learnings for future iterations:**
  - `run_profile()` in `scheduler.py` is the central entry point for both scheduled (`_fire_profile`) and manual (`_run_and_release`) runs — wiring the sweep here covers both paths
  - Backfill exclusion is naturally achieved by gating on `kind != RunKind.BACKFILL` in `run_profile`
  - FakeLithosServer uses a FIFO queue for `list_responses` — adding a new `lithos_list` caller (sweep) can consume responses intended for other callers (feedback); prepend empty responses as needed
  - All tests pass: 760 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-014
- Created `tests/integration/test_backfill_exclusion.py` with 2 tests
- Positive test: POST /runs invokes `repair.sweep` exactly once — asserted via `patch("influx.scheduler.repair_sweep")` spy
- Negative test (AC-06-D): POST /backfills does NOT invoke `repair.sweep` — asserted via spy confirming zero calls
- Backfill exclusion logic already in place at `scheduler.py:133` (`kind != RunKind.BACKFILL`) from US-013
- Files changed: `tests/integration/test_backfill_exclusion.py` (created), `scripts/ralph/prd.json`
- **Learnings for future iterations:**
  - Patching `influx.scheduler.repair_sweep` as AsyncMock is the cleanest way to spy on sweep invocation without needing extra FakeLithosServer queue setup
  - When repair_sweep is mocked out, no need to prepend empty list_responses for the sweep's lithos_list call
  - All tests pass: 762 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-015
- Created `tests/integration/test_coordinator_slot.py` with 4 tests in `TestCoordinatorSlotIntegration`
- `test_sweep_runs_inside_coordinator_lock`: patches sweep and item_provider as spies, both observe `coordinator.is_busy(profile) == True` during their execution
- `test_lock_held_exactly_once_per_profile_run`: wraps `coordinator.try_acquire` with a counter, asserts exactly 1 acquire per run (no double-lock)
- `test_two_profiles_run_independently`: launches POST /runs for profile-a and profile-b, verifies each sweep observes its own lock held
- `test_scheduled_fire_holds_lock_for_run_profile`: patches `run_profile` to observe lock state during `_fire_profile` scheduled path, confirms lock held
- No production code changes needed — coordinator integration was already correct in `scheduler.py` and `http_api.py` from US-013
- Files changed: `tests/integration/test_coordinator_slot.py` (created), `scripts/ralph/prd.json`
- **Learnings for future iterations:**
  - `_fire_profile` doesn't pass `item_provider`, so `run_profile` returns early (before sweep). For scheduled path tests, patch `run_profile` directly instead of `repair_sweep`
  - Ruff enforces SIM117 (combine nested `with` statements) — use `with (patch(...), TestClient(app) as tc):` syntax
  - All tests pass: 766 passed, pyright 0 errors, ruff clean
---

## 2026-04-25 - US-016
- Created `tests/integration/test_repair_sweep_archive_only.py` with 5 tests in `TestArchiveOnlyRepairFlow`
- Added `ArchiveDownloadHook` protocol to `src/influx/repair.py` — `(note) -> str` returns archive path
- Added `archive_download` field to `SweepHooks` (first field, None default)
- Wired archive download hook in `_process_sweep_note()`: on success, updates `archive_path` and inserts `path:` line into note's `## Archive` section content
- Added abstract-only re-extraction re-evaluation after archive download (handles case where archive success makes abstract-only eligible mid-pass)
- Tests cover: exact empty initial `## Archive` body, exact post-repair `path:` line, AC-06-A (text:html + archive-missing → archive only), archive failure keeps tags, repair-needed kept when Tier 2/3 outstanding
- Files changed: `src/influx/repair.py`, `tests/integration/test_repair_sweep_archive_only.py` (created), `scripts/ralph/prd.json`
- **Learnings for future iterations:**
  - `ArchiveDownloadHook` uses `Protocol` like other hooks; pyright enforces parameter name matching for Protocol callables (use `note` not `n`)
  - Content update for `## Archive` uses string `find` + `insert` approach: find `"## Archive\n"` marker, insert `"path: <path>\n"` after it
  - Integration tests call `sweep()` directly with `LithosClient` + `FakeLithosServer` — queue list, read, write responses via `_queue_single_note()` helper
  - Low-score notes (score < thresholds) naturally bypass Tier 2/3 clearing requirements, making archive-only repair testable
  - All tests pass: 771 passed, pyright 0 errors, ruff clean
---
